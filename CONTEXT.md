# 定时简报任务（schedule/）

有连续性需求的定时检索/简报任务的统一规范域：每个任务一个主题目录，跨运行靠文件维持记忆。本文件只是术语表，不含实现细节。

## Language

**任务定义（SKILL.md）**:
一个定时任务的唯一权威行为定义，存放于状态仓库内该任务目录：frontmatter（name/description）+ 运行时正文。它是行为定义的**唯一副本**——调度器不持有副本，每次运行现读现执行。
_Avoid_: 任务文档、README（它不是"关于任务的说明"，它就是 prompt 本身）

**状态仓库（state repo）**:
承载全部任务定义、运行记忆与归档的 git 仓库；main 分支是唯一可信状态，未推送到 main 的改动对定时运行不生效，本地检出只是工作副本。
_Avoid_: 备份仓库、同步盘

**Routine（调度器）**:
云端定时执行单元；绑定状态仓库，只决定"何时跑"和"跑哪个任务"，永不承载任务行为定义。其 prompt 是一份**任务指针**——只说明去状态仓库读哪个任务定义并照它执行，不复述任何行为规则；因此调度器与状态仓库之间不存在需要对齐的两份内容。
_Avoid_: 触发器（旧的 app 侧机制）、cron 任务、prompt 副本

**运行时正文（runtime body）**:
任务定义中 frontmatter 之后的全部内容，即一次运行实际执行的那段指令；不得包含装配/维护说明，也不得包含 frontmatter。

**产品（run product）**:
一次运行面向用户的交付物：会话的最后一条消息，其正文是本次归档文件的逐字副本，之后至多追加一行运行备注。指向归档的指针（"详见仓库"）、摘要、完成播报都不构成产品。
_Avoid_: 输出、回复（泛指，不区分中间过程与交付物）

**格式契约（output contract）**:
以字面骨架（围栏代码块）定义的产出结构，全部任务共用同一份，任务之间只有板块名不同。用代码块而非散文描述，是因为散文里的强调排版会被当作产出格式抄走。契约只约束结构，不约束内容取舍。
_Avoid_: 输出模板、样例（会被理解为可自由发挥的参考）

**归档（archive）**:
一次成功运行产出的人类可读结果快照，按日期命名、只增不改、永久保留；任何清理只由用户手动进行，不属于运行流程。
_Avoid_: 日志、历史记录、备份、保留期

**运行记忆（MEMORY.md）**:
任务目录下供下一次运行读取的机器记忆：上次运行时刻与实际覆盖窗口、近期已报条目清单、进行中事件追踪表。每次运行开头读取、结尾整体重写；缺失或损坏时降级为读最近 1–3 份归档重建，矛盾时以归档为准。
_Avoid_: 状态文件、state.json、缓存

**已报条目（covered item）**:
运行记忆中记录的一条已报道内容：日期 + 一句话事件描述（谁+干了什么）+ 代表性链接。同一性按事件级语义判定——同一事件不同媒体/标题/链接仍算同一条。
_Avoid_: 唯一标识（标题/链接/编号）（字面匹配写法）

**进行中事件（open thread）**:
已报道过、且预期后续还有进展、需要跨运行持续追踪的事件；在运行记忆中登记"事件、最后进展日期、关注点"，闭合后移除。
_Avoid_: 待办、watchlist

**续报（【续报】）**:
对进行中事件的新进展条目所加的统一前缀标记；只写增量，不重复旧细节。
_Avoid_: [UPDATE]（除非该任务整体为英文输出）

**传闻（【传闻】）**:
来自有可信度的爆料源、尚未获官方证实的条目所加的统一前缀标记；条目须写明消息出自谁，不得表述为既成事实；默认登记为进行中事件，证实或辟谣后以续报闭合。
_Avoid_: 小道消息（口语，不作标记用）、[RUMOR]（除非该任务整体为英文输出）

**覆盖窗口（coverage window）**:
一次运行负责收录的内容时间范围：自上次运行（取自运行记忆）至本次运行时刻，封顶 N 天（模板默认 7，任务可改）；首次运行按任务定义的默认窗口。缺口超过封顶时，窗口外只收至今仍重要的大事，且须在产出开头注明实际覆盖范围。
_Avoid_: 过去 24 小时（绝对时长写法，与调度频率耦合）

**运行备注**:
仅当运行出现异常或特殊动作（记忆重建、部分方向未覆盖、封顶截断、事件表因上限踢出）时，附在产品末尾的一行普通文字；它描述运行状态而非内容，**不进归档**；正常运行不输出任何结尾总结。推送走哪条路径不属于异常，不写入。
_Avoid_: run-summary、<run-summary> 标签（尖括号标签在聊天界面渲染成疑似 bug 的原始文本，且无机器消费者）

**看门狗（watchdog）**:
状态仓库内独立于调度器的归档检测：每天核对各任务当天该有的归档是否存在、且是否满足格式契约中可机械判定的部分，不合格即开 Issue 告警。它只观测归档这一个事实，不关心调度器为何没产出（静默停摆或运行失败一视同仁），因此不依赖调度平台的任何接口或状态；校验规则一律限定为正则可判定的结构，绝不涉及内容质量判断。
_Avoid_: 健康检查、心跳（它不探活，只查产出）

**topic-slug**:
任务的唯一标识，英文小写连字符命名，同时用作目录名、Routine 名与归档文件名前缀。
